function start

	var a := 1000
	const b := " mal berührt"

	var d : double(1000)
	const c : char('x')
	println(d, c)

	// tuples
	var ab := a, b
	var A, B := ab
	A, B := ab
	a, B := A, b
	println(A, B)

	return 0
end
